# INTENTIONALLY EMPTY — NO pkgbase base packages, build-time or otherwise.
#
# Per the project rule (no base packages, build everything from source):
# anything the chroot needs to COMPILE the Apple stack must come from either
#   - nextbsd-freebsd-compat (the from-source base: libc/libthr/clibs members
#     + full /usr/include + static libs + crt objects, overlaid onto rootfs), or
#   - the kernel toolchain image / ports (cmake/ninja/pkgconf/llvm19 in
#     buildpkgs.txt — ports, not base).
#
# History: this file used to list FreeBSD-clang/-lld/-toolchain/-clang-dev
# (now ports llvm19) and the -dev header/static-lib packages
# (FreeBSD-runtime-dev, -clibs-dev, -libcompiler_rt-dev, -libcompat-dev,
# -libexecinfo-dev, -openssl-dev, -audit-dev). All removed. If a build breaks
# for a missing header/lib, the fix is to add the corresponding lib/secure
# dir to the compat srclist (build it from source) — NOT to re-add a base pkg.
#
# Kept as an empty (comment-only) file so build.sh's grep yields no entries
# and BASE_BUILD_PKGS stays empty; slated for deletion once the peel settles.
