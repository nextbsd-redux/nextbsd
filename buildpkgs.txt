# One pkg per line; lines starting with # are ignored.
# Build packages — installed in the chroot to compile our vendored sources
# (libdispatch via cmake). Kept in the shipped image (the end-of-build purge
# was removed); the pkg download cache is still cleared.
cmake
ninja
# CMake's find_package module-mode probes use pkg-config under the
# hood — keep pkgconf so libdispatch's cmake discovery works.
pkgconf

# clang/clang++/lld + binutils from ports (devel/llvm19) — the in-chroot
# build compiler. build.sh symlinks the unprefixed tool names
# (cc/ld/ar/...) from /usr/local/llvm19/bin into /usr/local/bin so
# make/cmake/kmod builds pick it up.
llvm19
