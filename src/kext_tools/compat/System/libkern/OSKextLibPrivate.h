/*
 * System/libkern/OSKextLibPrivate.h — NextBSD compat stub for the OSKext port (#182, Phase 0).
 * kext flags/types — populate per the error surface.
 * Minimal placeholder so the #include resolves; populated per the
 * compile-check error surface, or the consuming code path is #ifdef-ed
 * out when it is XNU-only (kxld / mkext / prelink / kext_request).
 */
#ifndef _NEXTBSD_COMPAT_OSKEXTLIBPRIVATE_H
#define _NEXTBSD_COMPAT_OSKEXTLIBPRIVATE_H
#endif
